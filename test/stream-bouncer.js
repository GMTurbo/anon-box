var fs = require('fs');
var should = require('chai').should();
var StreamBouncer = require('../lib/stream-bouncer');

describe('StreamBouncer', function(){
  describe('#push()', function(){
    it('sup', function() {
      // StreamBouncer.should()
    });
  });
});


